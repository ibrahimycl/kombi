# Exclude heavy / host-specific files from the Docker build context.
# Without this the committed node_modules/ would be sent to the
# Docker daemon on every build — slow and pointless.
node_modules
dist
.git
.gitignore
*.log
.DS_Store
.env
.env.*
README.md
